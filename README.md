# ⚡ Sobel Filter: WebAssembly vs JavaScript

Проект демонстрирует сравнение производительности между **WebAssembly (AssemblyScript)** и нативным **JavaScript** на примере алгоритма выделения границ (фильтр Собеля).

Приложение обрабатывает изображения в реальном времени, показывая разницу в скорости вычислений при манипуляции пикселями и прямом доступе к памяти.

## 🛠 Технологии
- **AssemblyScript:** Компиляция TypeScript в WebAssembly для высокой производительности.
- **JavaScript (ESM):** Логика интерфейса и эталонная реализация алгоритма.
- **HTML5 Canvas:** Отрисовка результатов.
- **Tailwind CSS:** Стилизация интерфейса.

---

## 🚀 Инструкция по запуску

1. Скачайте архив проекта.
2. Распакуйте его в любом удобном месте.
3. Скачайте и установите **[Node.js](https://nodejs.org/)** (если еще не установлен).
4. В `cmd` (или терминале) проверьте правильность установки:
   ```bash
   node -v
   npm -v
5. Если все верно, вы должны увидеть номера версий.

6. В папке проекта выполните команды по очереди:
      ```bash
      npm install      # установка зависимостей  
      npm run asbuild  # компиляция WebAssembly  
      npx serve .      # быстрый старт локального сервера
7. Откройте браузер и введите адрес: http://localhost:3000

8. Выберите фото (рекомендуется высокое разрешение) и сравните время выполнения JS и WASM.

9. При необходимости перезагрузите страницу для сброса.
